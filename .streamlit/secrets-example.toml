SUPABASE_URL = "https://your-project.supabase.co"
SUPABASE_KEY = "your-anon-key"

# Client Alerts page: pseudonym code lookups, in place of the old
# *.local.json files (gitignored, so never present after deployment).
# slug -> pseudonym code, code -> assigned employee ids, employee id -> name.
[client_pseudonyms]
example-company = "CL-0001"

[client_employees]
CL-0001 = ["EMP-001"]

[employee_names]
EMP-001 = "Jane Doe"